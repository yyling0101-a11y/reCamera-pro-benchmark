# Artifacts — PT and ONNX intermediate files (not published)
artifacts/
doc/

# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg

# Conda/Venv
.conda/
env/
venv/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Calibration data (not needed for repo; scripts download from official sources)
calibration/

# Benchmark temp files
*.tmp
*.lock

# Large PDFs in doc/ (keep only if small)
# doc/ is kept (only contains the Rockchip PDF reference)

output/
# GitHub has a 100 MB per-file hard limit (kept locally only)
models/zipformer/zipformer_encoder_official_FP16.rknn

# ONNX-graph debug snapshots produced during optimization (kept locally only)
check[0-9]*.onnx
